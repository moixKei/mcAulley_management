<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Lista de Alumnas - McAulley</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>🎓 Lista de Alumnas</h1>
        
        <a th:href="@{/alumnas/nuevo}" class="btn btn-success mb-3">➕ Nueva Alumna</a>
        
        <div th:if="${listaAlumnas.empty}">
            <div class="alert alert-info">No hay alumnas registradas</div>
        </div>
        
        <table th:unless="${listaAlumnas.empty}" class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>DNI</th>
                    <th>Correo</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="alumna : ${listaAlumnas}">
                    <td th:text="${alumna.id}"></td>
                    <td th:text="${alumna.nombre}"></td>
                    <td th:text="${alumna.apellido}"></td>
                    <td th:text="${alumna.dni}"></td>
                    <td th:text="${alumna.correo}"></td>
                    <td>
                        <a th:href="@{/alumnas/detalle/} + ${alumna.id}" class="btn btn-info btn-sm">👁️ Ver</a>
                    </td>
                </tr>
            </tbody>
        </table>
        
        <a th:href="@{/}" class="btn btn-secondary">🏠 Inicio</a>
    </div>
</body>
</html>