<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Curso</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1><i class="fas fa-info-circle me-2"></i>Detalle del Curso</h1>
            <a th:href="@{/cursos}" class="btn btn-secondary">
                <i class="fas fa-arrow-left me-1"></i>Volver a la lista
            </a>
        </div>

        <!-- Alertas -->
        <div th:if="${error}" class="alert alert-danger alert-dismissible fade show" role="alert">
            <span th:text="${error}"></span>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>

        <div th:object="${curso}" class="row">
            <!-- Información Principal -->
            <div class="col-md-8">
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">
                            <i class="fas fa-book me-1"></i>
                            Información General
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>ID:</strong> <span th:text="*{idCurso}"></span></p>
                                <p><strong>Nombre:</strong> <span th:text="*{nombre}"></span></p>
                                <p><strong>Duración:</strong> 
                                    <span th:text="*{duracionHoras} ?: 'N/A'"></span> horas
                                </p>
                                <p><strong>Precio:</strong> 
                                    <span th:if="*{precio > 0}" 
                                          th:text="'$' + ${#numbers.formatDecimal(curso.precio, 1, 2)}"
                                          class="badge bg-success">
                                    </span>
                                    <span th:unless="*{precio > 0}" 
                                          class="badge bg-secondary">Gratis</span>
                                </p>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Cupo Máximo:</strong> <span th:text="*{cupoMaximo}"></span> estudiantes</p>
                                <!-- En detalle.html, actualiza esta parte -->
								<p><strong>Estado:</strong> 
								    <span th:if="${curso.estado != null}" 
								          th:text="${curso.estado.nombre}"
								          th:classappend="${curso.estado.nombreEstado  == 'Activo'} ? 'badge bg-success' : 
								                          ${curso.estado.nombreEstado  == 'Inactivo'} ? 'badge bg-danger' : 
								                          'badge bg-warning'">
								    </span>
								    <span th:if="${curso.estado == null}" class="badge bg-secondary">
								        No asignado
								    </span>
								</p>
                                <p><strong>Activo:</strong> 
                                    <span th:if="*{activo}" class="badge bg-success">
                                        <i class="fas fa-check"></i> Sí
                                    </span>
                                    <span th:unless="*{activo}" class="badge bg-danger">
                                        <i class="fas fa-times"></i> No
                                    </span>
                                </p>
                                <p><strong>Fecha Creación:</strong> 
                                    <span th:text="*{fechaCreacion} ?: 'No especificada'"></span>
                                </p>
                            </div>
                        </div>
                        
                        <!-- Descripción -->
                        <div class="mt-3">
                            <strong>Descripción:</strong>
                            <div class="border rounded p-3 mt-2 bg-light">
                                <p th:text="*{descripcion} ?: 'No hay descripción disponible'" 
                                   class="mb-0"></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Fechas y Acciones -->
            <div class="col-md-4">
                <!-- Fechas -->
                <div class="card mb-4">
                    <div class="card-header bg-info text-white">
                        <h6 class="mb-0"><i class="fas fa-calendar me-1"></i>Fechas</h6>
                    </div>
                    <div class="card-body">
                        <p><strong>Inicio:</strong> 
                            <span th:text="*{fechaInicio} ?: 'No especificada'"></span>
                        </p>
                        <p><strong>Fin:</strong> 
                            <span th:text="*{fechaFin} ?: 'No especificada'"></span>
                        </p>
                        <p><strong>Creación:</strong> 
                            <span th:text="*{fechaCreacion} ?: 'No especificada'"></span>
                        </p>
                    </div>
                </div>

                <!-- Acciones -->
                <div class="card">
                    <div class="card-header bg-warning">
                        <h6 class="mb-0"><i class="fas fa-cogs me-1"></i>Acciones</h6>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a th:href="@{/cursos/editar/{id}(id=${curso.idCurso})}" 
                               class="btn btn-warning btn-sm">
                                <i class="fas fa-edit me-1"></i>Editar Curso
                            </a>
                            <form th:action="@{/cursos/eliminar/{id}(id=${curso.idCurso})}" 
                                  method="post" class="d-grid"
                                  th:onsubmit="return confirm('¿Está seguro de eliminar este curso?');">
                                <button type="submit" class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash me-1"></i>Eliminar Curso
                                </button>
                            </form>
                            <a th:href="@{/cursos}" class="btn btn-secondary btn-sm">
                                <i class="fas fa-list me-1"></i>Ver Todos los Cursos
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>