<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Lista de Alumnas - McAulley</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <!-- Header con estadísticas -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1>🎓 Gestión de Alumnas</h1>
                <p class="text-muted" th:if="${totalAlumnas != null}">
                    Total: <span th:text="${totalAlumnas}"></span> | 
                    Activas: <span th:text="${alumnasActivas}"></span>
                </p>
            </div>
            <div>
                <a th:href="@{/alumnas/estadisticas}" class="btn btn-outline-info me-2">
                    <i class="fas fa-chart-bar"></i> Estadísticas
                </a>
                <a th:href="@{/alumnas/nuevo}" class="btn btn-success">
                    <i class="fas fa-plus"></i> Nueva Alumna
                </a>
            </div>
        </div>

        <!-- Mensajes de éxito/error -->
        <div th:if="${success}" class="alert alert-success alert-dismissible fade show" role="alert">
            <span th:text="${success}"></span>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        <div th:if="${error}" class="alert alert-danger alert-dismissible fade show" role="alert">
            <span th:text="${error}"></span>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>

        <!-- Panel de búsqueda y filtros -->
        <div class="card mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="fas fa-search"></i> Búsqueda y Filtros</h5>
            </div>
            <div class="card-body">
                <form th:action="@{/alumnas/buscar}" method="get" class="row g-3">
                    <div class="col-md-3">
                        <input type="text" name="nombre" class="form-control" placeholder="Nombre" th:value="${filtroNombre}">
                    </div>
                    <div class="col-md-3">
                        <input type="text" name="apellido" class="form-control" placeholder="Apellido" th:value="${filtroApellido}">
                    </div>
                    <div class="col-md-2">
                        <input type="text" name="dni" class="form-control" placeholder="DNI" th:value="${filtroDni}">
                    </div>
                    <div class="col-md-2">
                        <select name="activa" class="form-select">
                            <option value="">Todos los estados</option>
                            <option value="true" th:selected="${filtroActiva == true}">Activas</option>
                            <option value="false" th:selected="${filtroActiva == false}">Inactivas</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-search"></i> Buscar
                        </button>
                    </div>
                </form>
                
                <!-- Filtros rápidos -->
                <div class="mt-3">
                    <a th:href="@{/alumnas}" class="btn btn-sm btn-outline-secondary me-1">Todas</a>
                    <a th:href="@{/alumnas/activas}" class="btn btn-sm btn-outline-success me-1">Solo Activas</a>
                    <span th:if="${filtro}" class="badge bg-info">
                        Filtro: <span th:text="${filtro}"></span>
                        <a th:href="@{/alumnas}" class="text-white ms-1">×</a>
                    </span>
                    <span th:if="${totalResultados != null}" class="badge bg-primary ms-2">
                        Resultados: <span th:text="${totalResultados}"></span>
                    </span>
                </div>
            </div>
        </div>

        <!-- Tabla de alumnas -->
        <div class="card">
            <div class="card-header bg-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><i class="fas fa-list"></i> Lista de Alumnas</h5>
                <span class="badge bg-secondary" th:text="'Total: ' + ${listaAlumnas.size()}"></span>
            </div>
            <div class="card-body p-0">
                <div th:if="${listaAlumnas.empty}" class="text-center py-5">
                    <i class="fas fa-users fa-3x text-muted mb-3"></i>
                    <h5 class="text-muted">No se encontraron alumnas</h5>
                    <p class="text-muted">No hay alumnas que coincidan con los criterios de búsqueda</p>
                    <a th:href="@{/alumnas/nuevo}" class="btn btn-primary">Registrar primera alumna</a>
                </div>

                <table th:unless="${listaAlumnas.empty}" class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>ID</th>
                            <th>Nombre Completo</th>
                            <th>DNI</th>
                            <th>Contacto</th>
                            <th>Fecha Registro</th>
                            <th>Estado</th>
                            <th width="200">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr th:each="alumna : ${listaAlumnas}">
                            <td th:text="${alumna.idAlumna}"></td>
                            <td>
                                <strong th:text="${alumna.nombre + ' ' + alumna.apellido}"></strong>
                            </td>
                            <td>
                                <code th:text="${alumna.dni}"></code>
                            </td>
                            <td>
                                <div th:if="${alumna.correo}">
                                    <i class="fas fa-envelope text-muted"></i>
                                    <small th:text="${alumna.correo}"></small>
                                </div>
                                <div th:if="${alumna.celular}">
                                    <i class="fas fa-phone text-muted"></i>
                                    <small th:text="${alumna.celular}"></small>
                                </div>
                            </td>
                            <td>
                                <small class="text-muted" th:text="${#temporals.format(alumna.fechaRegistro, 'dd/MM/yyyy')}"></small>
                            </td>
                            <td>
                                <span th:if="${alumna.activo}" class="badge bg-success">
                                    <i class="fas fa-check"></i> Activa
                                </span>
                                <span th:unless="${alumna.activo}" class="badge bg-secondary">
                                    <i class="fas fa-times"></i> Inactiva
                                </span>
                            </td>
                            <td>
                                <div class="btn-group btn-group-sm" role="group">
                                    <a th:href="@{/alumnas/detalle/} + ${alumna.idAlumna}" 
                                       class="btn btn-outline-info" title="Ver detalle">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a th:href="@{/alumnas/editar/} + ${alumna.idAlumna}" 
                                       class="btn btn-outline-warning" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    
                                    <!-- Toggle Estado -->
                                    <form th:action="@{/alumnas/toggle-estado/} + ${alumna.idAlumna}" 
                                          method="post" class="d-inline">
                                        <button type="submit" 
                                                th:classappend="${alumna.activo} ? 'btn-outline-warning' : 'btn-outline-success'"
                                                class="btn" 
                                                th:title="${alumna.activo} ? 'Desactivar' : 'Activar'">
                                            <i th:classappend="${alumna.activo} ? 'fa-toggle-on' : 'fa-toggle-off'" 
                                               class="fas"></i>
                                        </button>
                                    </form>
                                    
                                    <!-- Eliminar -->
                                    <button type="button" 
                                            class="btn btn-outline-danger" 
                                            title="Eliminar"
                                            data-bs-toggle="modal" 
                                            th:attr="data-bs-target='#modalEliminar' + ${alumna.idAlumna}">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>

                                <!-- Modal de confirmación para eliminar -->
                                <div class="modal fade" th:id="'modalEliminar' + ${alumna.idAlumna}" tabindex="-1">
                                    <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title">Confirmar Eliminación</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                            </div>
                                            <div class="modal-body">
                                                <p>¿Estás seguro de que deseas eliminar a la alumna?</p>
                                                <div class="alert alert-warning">
                                                    <strong th:text="${alumna.nombre + ' ' + alumna.apellido}"></strong><br>
                                                    <small>DNI: <code th:text="${alumna.dni}"></code></small>
                                                </div>
                                                <p class="text-danger"><small>Esta acción no se puede deshacer.</small></p>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                                                <form th:action="@{/alumnas/eliminar/} + ${alumna.idAlumna}" method="post">
                                                    <button type="submit" class="btn btn-danger">Eliminar</button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Operaciones batch -->
        <div class="mt-3 text-end">
            <form th:action="@{/alumnas/desactivar-antiguas}" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-warning btn-sm">
                    <i class="fas fa-clock"></i> Desactivar Alumnas Antiguas
                </button>
            </form>
            <a th:href="@{/}" class="btn btn-secondary btn-sm ms-2">
                <i class="fas fa-home"></i> Inicio
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-dismiss alerts after 5 seconds
        setTimeout(function() {
            var alerts = document.querySelectorAll('.alert');
            alerts.forEach(function(alert) {
                var bsAlert = new bootstrap.Alert(alert);
                bsAlert.close();
            });
        }, 5000);
    </script>
</body>
</html>